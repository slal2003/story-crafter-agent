# Story Crafter Agent Configuration

# ============================================================================
# AUTHENTICATION - Choose ONE of the following options:
# ============================================================================

# OPTION 1: Google Gemini API (Pay-as-you-go with API key)
# Get your API key from: https://ai.google.dev/
# This uses the free tier by default, with some rate limits
GOOGLE_API_KEY=your-gemini-api-key-here

# OPTION 2: Google Vertex AI (Requires GCP account with paid quota)
# IMPORTANT: When using Vertex AI, set this to True to use your paid quota
# Otherwise, it will hit the free tier rate limits (429 RESOURCE_EXHAUSTED error)
GOOGLE_GENAI_USE_VERTEXAI=True

# Vertex AI Configuration (REQUIRED if using GOOGLE_GENAI_USE_VERTEXAI=True)
# Get these from your Google Cloud Console: https://console.cloud.google.com/
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GOOGLE_CLOUD_LOCATION=us-central1

# Application Default Credentials (for Vertex AI)
# If using Vertex AI, ensure you have credentials set up:
#   gcloud auth application-default login
# OR set the path to your service account JSON:
#   export GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# ============================================================================
# OPTIONAL: Image Generation
# ============================================================================

# Airbrush.ai API for image generation (optional - system works without it)
AIRBRUSH_API_KEY=your-airbrush-api-key-here
AIRBRUSH_BASE_URL=https://app.airbrush.ai

# ============================================================================
# OPTIONAL: Book Data APIs
# ============================================================================

# Project Gutenberg API Key (if available)
GUTENBERG_SUMMARIES_API_KEY=your-api-key-here

# Custom book data API (if available)
PHASE1_API_KEY=your-api-key-here
